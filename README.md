RustyNES
===
A Nintendo Entertainment System (NES) emulator written in Rust
---

### Requirements

* Rust (duh)
* Cargo

### Installation

`$ cargo build`

### Usage

`$ rustynes [ROM file]`

#### Options

* `--mode <mode>`
	* `run` (default)
	* `nestest` - Run with nestest log output